import { Parts } from "./parts"
export class Sell{
    sellItemId ? : Number
    buyerName ? : String
    buyerPhoneNumber ? : String
    buyerTinNumber ? : String
    parts ? : Parts[]
    grandTotal ? : Number
}
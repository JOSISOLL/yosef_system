import { Parts } from "./parts"
export class Sell{
    sellItemId ? : Number
    buyerName ? : String
    buyerPhoneNumber ? : String
    buyerTinNumber ? : String
    personInCharge ? : String
    parts ? : Parts[]
    quantity ? : Number
    grandTotal ? : Number
}
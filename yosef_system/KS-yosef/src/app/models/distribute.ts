import { Imported_Parts } from "./imported_parts"
export class Distribute{
    buyerName ? : String
    buyerPhoneNumber ? : String
    buyerTinNumber ? : String
    personInCharge ? : String
    parts ? : Imported_Parts[]
    quantity ? : Number
    subTotal ? : Number
    grandTotal ? : Number
    date ? : Date
}
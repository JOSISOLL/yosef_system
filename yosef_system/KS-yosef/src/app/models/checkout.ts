import { Repair } from "./repair"
export class Checkout{
    repair ? : Repair 
    serviceCharge ? : Number
    problemsFixed ? : String
    tinNumber ? : String
    changedParts ? : String[]
    menInCharge ? : String[]
    checkoutDate ? : String
}